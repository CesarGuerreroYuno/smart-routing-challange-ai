# Smart Routing Blackout — environment variables
#
# This project has NO required environment variables.
# All data is generated client-side using a seeded LCG PRNG (seed = 42).
# Copy this file to .env if you need to add variables in the future.

# VITE_SIMULATION_SEED=42   # Optional: override the data generation seed
